# Next steps

Scratchpad for the current ask — what to build next, instructions for
Claude. Once work here is done, its outcome gets archived to
[`docs/log/`](log/README.md) (via the `project-log` skill) rather than kept
here, so this file only ever reflects what's currently in flight.
